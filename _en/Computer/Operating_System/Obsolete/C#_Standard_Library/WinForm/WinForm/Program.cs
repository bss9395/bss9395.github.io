/* CS_CheckBox.cs
Author: BSS9395
Update: 2022-04-30T23:51:00+08@China-Shanghai+08
Design: C# Standard Library: CheckBox
*/

using System;
using System.Windows.Forms;

class Program {
    [STAThread]
    public static void Main() {
        Application.EnableVisualStyles();
        Application.SetCompatibleTextRenderingDefault(false);
        // Application.Run(new CS_MessageBox());
        // Application.Run(new CS_Label());
        // Application.Run(new CS_LinkLabel());
        // Application.Run(new CS_TextBox());
        // Application.Run(new CS_Button());
        // Application.Run(new CS_RadioButton());
        // Application.Run(new CS_CheckBox());
        // Application.Run(new CS_CheckedListBox());
        // Application.Run(new CS_ListBox());
        // Application.Run(new CS_ComboBox());
        // Application.Run(new CS_PictureBox());
        // Application.Run(new CS_Timer());
        // Application.Run(new CS_DateTimePicker());
        // Application.Run(new CS_MonthCalendar());
        // Application.Run(new CS_ContextMenuStrip());
        // Application.Run(new CS_MenuStrip());
        // Application.Run(new CS_StatusStrip());
        // Application.Run(new CS_ToolStrip());
        // Application.Run(new CS_MDI());
        // Application.Run(new CS_ColorDialog());
        // Application.Run(new CS_FontDialog());
        // Application.Run(new CS_OpenFileDialog());
        Application.Run(new CS_SaveFileDialog());

    }
}

